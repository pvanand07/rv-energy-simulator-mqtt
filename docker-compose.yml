services:
  simulator:
    build: .
    command: python simulator.py
    ports:
      - "${SIMULATOR_PORT}:5001"   # Configurator UI
      - "${MQTT_PORT}:1883"        # Embedded MQTT broker
    volumes:
      - sim-data:/app/data
    restart: unless-stopped

  dashboard:
    build: .
    command: python broker_dashboard.py
    ports:
      - "${DASHBOARD_PORT}:5002"   # Real-time dashboard UI
    environment:
      - MQTT_HOST=simulator
      - MQTT_PORT=1883
    depends_on:
      - simulator
    restart: unless-stopped

volumes:
  sim-data:
